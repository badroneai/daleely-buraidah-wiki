/**
 * دليل بريدة الشامل — نقطة الدخول الرئيسية (ES Module)
 *
 * يستورد router.js الذي بدوره يستورد كل الوحدات المطلوبة
 * ويقوم بتهيئة التطبيق تلقائياً (loadData → bindSidebar → router).
 *
 * الملف الأصلي (script.js) محفوظ كنسخة احتياطية في script.original.js
 */

import './modules/router.js';
